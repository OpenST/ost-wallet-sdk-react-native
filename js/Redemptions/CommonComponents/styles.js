import { StyleSheet } from 'react-native';

let stylesMap = StyleSheet.create({
    balanceText :{
        color: '#535353',
        fontSize: 14,
        fontWeight: '500'
    }
});

export default styles = stylesMap;